
// Project status is now a flexible text field that uses custom statuses from project_statuses table
export type ProjectStatus = string;

// Define type for project stage as string
export type ProjectStage = string;
